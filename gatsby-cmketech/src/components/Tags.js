import React from "react"
import { Link } from "gatsby"

const Tags = ({}) => {
    return (

        // Below is a sample code of how we will loop in different tags from wordpress 
        // There's more to this COMING SOON!
        // <ul>
        //     return (
        //     <li key={slug}>
        //         <Link to={slug}>{title}</Link>
        //     </li>
        //     )
        // </ul>
        <ul>
            <li className="tag tag--gold tag--gold--featured">HR/Admin</li>
        </ul>
    )
}

export default Tags